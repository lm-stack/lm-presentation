---
// src/components/slides/Form.astro
// Slide "formulaire" ExecEd-style : header standard (brand + logo + border,
// 44/40), à gauche titre + contexte, à droite une maquette de formulaire
// (champs + bouton). Maquette statique (pas d'inputs interactifs, pour ne pas
// capter le clavier de Reveal). Charte LM light.
import { splitHighlight } from '@/utils/highlight';
interface Props {
  title?: string;
  italicPart?: string;
  subtitle?: string;
  /** Libellés des champs affichés. */
  fields?: string[];
  /** Libellé du bouton. */
  button?: string;
  brand?: string;
  brandSub?: string;
}
const {
  title = 'Un formulaire',
  italicPart,
  subtitle,
  fields = ['Nom', 'Prénom', 'Email'],
  button = 'Envoyer',
  brand = 'Titre de la présentation',
  brandSub,
} = Astro.props;
const titleParts = splitHighlight(title, italicPart);
---
<section class="form" data-layout="form">
  <header class="form__header">
    <div class="form__brand-text">
      <p class="form__brand-main">{brand}</p>
      {brandSub && <p class="form__brand-sub">{brandSub}</p>}
    </div>
    <a href="https://lausanne.marketing" target="_blank" rel="noopener" class="form__brand-logo-link" aria-label="Lausanne Marketing">
      <img src="/assets/lm-logo.svg" class="form__brand-logo" alt="Lausanne Marketing" />
    </a>
  </header>

  <div class="form__content">
    <div class="form__text">
      <h2 class="form__title">
        {titleParts
          ? <>{titleParts.before}<em class="form__title-italic"><span class="underline">{titleParts.match}</span></em>{titleParts.after}</>
          : title}
      </h2>
      {subtitle && <p class="form__subtitle">{subtitle}</p>}
    </div>

    <div class="form__card" aria-hidden="true">
      {fields.map((f) => (
        <div class="form__field">
          <span class="form__label">{f}</span>
          <span class="form__input"></span>
        </div>
      ))}
      <span class="form__button">{button}</span>
    </div>
  </div>
</section>

<style>
  .form {
    position: relative;
    width: 100%;
    height: 100%;
    overflow: hidden;
    padding: 44px 96px 64px !important;
    background: linear-gradient(180deg, var(--c-bg-start) 0%, var(--c-bg-end) 100%);
    color: var(--c-ink);
    display: flex !important;
    flex-direction: column !important;
    justify-content: flex-start !important;
  }

  .form__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-bottom: 40px;
    border-bottom: 1px solid color-mix(in srgb, var(--c-ink) 12%, transparent);
  }
  .form__brand-main {
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: var(--fs-brand);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    margin: 0;
    color: var(--c-ink);
    text-align: left;
  }
  .form__brand-sub {
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: var(--fs-brand-sub);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    margin: 4px 0 0;
    color: var(--c-muted);
    text-align: left;
  }
  .form__brand-logo {
    height: 48px;
    width: auto;
    display: block;
    flex: 0 0 auto;
  }

  .form__content {
    flex: 1 1 auto;
    min-height: 0;
    display: flex;
    align-items: center;
    gap: 96px;
  }
  .form__text {
    flex: 1 1 0;
    min-width: 0;
  }
  .form__title {
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: var(--fs-hero-l);
    line-height: var(--lh-title);
    font-weight: 600;
    letter-spacing: -0.02em;
    margin: 0;
    color: var(--c-ink);
    text-wrap: balance;
    text-align: left;
  }
  .form__title-italic {
    font-family: 'Iowan Old Style', 'Palatino Linotype', Palatino, Georgia, 'Times New Roman', serif;
    font-style: italic;
    font-weight: 400;
  }
  .form__subtitle {
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: var(--fs-lead);
    line-height: var(--lh-lead);
    font-weight: 400;
    color: var(--c-muted);
    margin: 28px 0 0;
    max-width: var(--measure-s);
  }

  /* Maquette de formulaire. */
  .form__card {
    flex: 0 0 auto;
    width: 620px;
    max-width: 46%;
    background: var(--c-surface);
    border: 1px solid color-mix(in srgb, var(--c-ink) 8%, transparent);
    border-radius: 4px;
    box-shadow: 0 6px 16px -6px color-mix(in srgb, var(--c-ink) 12%, transparent);
    padding: 48px 48px 44px;
    display: flex;
    flex-direction: column;
    gap: 24px;
  }
  .form__field {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .form__label {
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: var(--fs-body);
    font-weight: 600;
    color: var(--c-ink);
  }
  .form__input {
    height: 56px;
    border: 1px solid color-mix(in srgb, var(--c-ink) 18%, transparent);
    border-radius: 4px;
    background: var(--c-cream);
    display: block;
  }
  .form__button {
    margin-top: 8px;
    align-self: stretch;
    text-align: center;
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: var(--fs-body);
    font-weight: 700;
    color: var(--c-on-accent);
    background: var(--c-accent);
    border-radius: 4px;
    padding: 16px 28px;
  }
</style>
