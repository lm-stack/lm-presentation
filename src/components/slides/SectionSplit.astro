---
// src/components/slides/SectionSplit.astro
// Layout split half-half : moitié gauche fond noir LM avec titre serif
// italique partiel + carré jaune sous-titre + body texte ; moitié droite
// light texturée (motif damier subtil). Pour les ouvertures de section
// avec un body explicatif riche, là où NumberedSplit attendait une liste.
// Inspiration slide 20 du PPTX ExecEd.
interface Props {
  title: string;
  italicPart?: string;
  subtitle?: string;
  body: string;
  brand?: string;
  brandSub?: string;
}
const {
  title,
  italicPart,
  subtitle,
  body,
  brand = 'Titre de la présentation',
  brandSub,
} = Astro.props;
---
<section class="section-split" data-layout="section-split">
  <div class="section-split__bg-right" aria-hidden="true"></div>

  <div class="section-split__left">
    <header class="section-split__header">
      <p class="section-split__brand-main">{brand}</p>
      {brandSub && <p class="section-split__brand-sub">{brandSub}</p>}
    </header>

    <div class="section-split__body">
      <h2 class="section-split__title">
        {italicPart
          ? <>{title.split(italicPart)[0]}<em class="section-split__title-italic">{italicPart}</em>{title.split(italicPart)[1]}</>
          : title}
      </h2>

      {subtitle && (
        <p class="section-split__subtitle">
          <span class="section-split__subtitle-square" aria-hidden="true"></span>
          {subtitle}
        </p>
      )}

      <p class="section-split__text">{body}</p>
    </div>
  </div>
</section>

<style>
  .section-split {
    position: relative;
    width: 100%;
    height: 100%;
    overflow: hidden;
    padding: 0 !important;
    background: linear-gradient(180deg, var(--c-bg-start) 0%, var(--c-bg-end) 100%);
    color: var(--c-ink);
    display: block !important;
  }

  .section-split__bg-right {
    position: absolute;
    top: 0;
    right: 0;
    width: 38%;
    height: 100%;
    background-image:
      linear-gradient(45deg, color-mix(in srgb, var(--c-ink) 5%, transparent) 25%, transparent 25%),
      linear-gradient(-45deg, color-mix(in srgb, var(--c-ink) 5%, transparent) 25%, transparent 25%),
      linear-gradient(45deg, transparent 75%, color-mix(in srgb, var(--c-ink) 5%, transparent) 75%),
      linear-gradient(-45deg, transparent 75%, color-mix(in srgb, var(--c-ink) 5%, transparent) 75%);
    background-size: 24px 24px;
    background-position: 0 0, 0 12px, 12px -12px, -12px 0;
  }

  .section-split__left {
    position: relative;
    z-index: 1;
    width: 62%;
    height: 100%;
    padding: 64px 96px;
    display: flex;
    flex-direction: column;
  }

  .section-split__header {
    padding-bottom: 24px;
    border-bottom: 1px solid color-mix(in srgb, var(--c-ink) 12%, transparent);
  }
  .section-split__brand-main {
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: 22px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    margin: 0;
    color: var(--c-ink);
  }
  .section-split__brand-sub {
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: 14px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    margin: 4px 0 0;
    color: var(--c-muted);
  }

  .section-split__body {
    flex: 1 1 auto;
    display: flex;
    flex-direction: column;
    justify-content: center;
    min-height: 0;
    padding-top: 32px;
    max-width: 880px;
  }

  .section-split__title {
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: clamp(72px, 7vw, 116px);
    line-height: 1.05;
    font-weight: 600;
    letter-spacing: -0.02em;
    margin: 0;
    color: var(--c-ink);
    text-wrap: balance;
    text-align: left;
  }
  .section-split__title-italic {
    font-family: 'Iowan Old Style', 'Palatino Linotype', Palatino, Georgia, 'Times New Roman', serif;
    font-style: italic;
    font-weight: 400;
  }

  .section-split__subtitle {
    display: flex;
    align-items: center;
    gap: 12px;
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: 26px;
    font-weight: 500;
    color: var(--c-muted);
    margin: 24px 0 32px;
  }
  .section-split__subtitle-square {
    width: 14px;
    height: 14px;
    background: var(--c-accent);
    display: inline-block;
    flex: 0 0 auto;
  }

  .section-split__text {
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: 22px;
    line-height: 1.55;
    color: var(--c-muted);
    margin: 0;
    max-width: 720px;
  }
</style>
