---
// src/components/slides/ExchangeHero.astro
// Slide "Échange" ExecEd-style : header standard (brand + logo LM + border, 44/40),
// à gauche une pastille "Échange" (carré jaune) + titre (Hanken Grotesk, portion
// en serif italique soulignée jaune) + sous-titre + liste optionnelle de points de
// discussion ; à droite un motif "discussion" (icône chats-circle posée dans un
// halo doré, écho du thème halo-gold de CoverHero). Charte LM light, fond pearl.
//
// Pendant ExecEd de la transition Exchange (ancien format, pleine page rouge) :
// un temps d'échange devient un slide de contenu cohérent avec la famille Hero
// (même header, même fond, même grammaire typographique que Demo/Workshop/Statement).
import { Icon } from 'astro-icon/components';

interface Props {
  title: string;
  /** Portion du titre en serif italique soulignée jaune. */
  italicPart?: string;
  subtitle?: string;
  /** Libellé de la pastille en haut à gauche. Défaut "Échange". */
  eyebrow?: string;
  /** Points de discussion optionnels, listés sous le sous-titre. */
  prompts?: string[];
  /** Icône Phosphor du motif de droite. Défaut "ph:chats-circle-fill". */
  icon?: string;
  brand?: string;
  brandSub?: string;
  tocLabel?: string;
}
const {
  title,
  italicPart,
  subtitle,
  eyebrow = 'Échange',
  prompts = [],
  icon = 'ph:chats-circle-fill',
  brand = 'Titre de la présentation',
  brandSub,
  tocLabel,
} = Astro.props;
---
<section class="exchange-hero" data-layout="exchange-hero" data-section-title={tocLabel ?? title}>
  <header class="exchange-hero__header">
    <div class="exchange-hero__brand-text">
      <p class="exchange-hero__brand-main">{brand}</p>
      {brandSub && <p class="exchange-hero__brand-sub">{brandSub}</p>}
    </div>
    <a href="https://lausanne.marketing" target="_blank" rel="noopener" class="exchange-hero__brand-logo-link" aria-label="Lausanne Marketing">
      <img src="/assets/lm-logo.svg" class="exchange-hero__brand-logo" alt="Lausanne Marketing" />
    </a>
  </header>

  <div class="exchange-hero__content">
    <div class="exchange-hero__text">
      <p class="exchange-hero__eyebrow">
        <span class="exchange-hero__eyebrow-square" aria-hidden="true"></span>{eyebrow}
      </p>
      <h2 class="exchange-hero__title">
        {italicPart
          ? <>{title.split(italicPart)[0]}<em class="exchange-hero__title-italic"><span class="underline">{italicPart}</span></em>{title.split(italicPart)[1]}</>
          : title}
      </h2>
      {subtitle && <p class="exchange-hero__subtitle">{subtitle}</p>}
      {prompts.length > 0 && (
        <ul class="exchange-hero__prompts">
          {prompts.map((p) => (
            <li class="exchange-hero__prompt">
              <span class="exchange-hero__prompt-mark" aria-hidden="true"></span>
              <span>{p}</span>
            </li>
          ))}
        </ul>
      )}
    </div>

    <div class="exchange-hero__visual" aria-hidden="true">
      <span class="exchange-hero__halo"></span>
      <span class="exchange-hero__icon">
        <Icon name={icon} />
      </span>
    </div>
  </div>
</section>

<style>
  .exchange-hero {
    position: relative;
    width: 100%;
    height: 100%;
    overflow: hidden;
    padding: 44px 96px 64px !important;
    background: linear-gradient(180deg, var(--c-bg-start) 0%, var(--c-bg-end) 100%);
    color: var(--c-ink);
    display: flex !important;
    flex-direction: column !important;
    justify-content: flex-start !important;
  }

  /* Header standard intangible : brand gauche + logo LM droite + border-bottom. */
  .exchange-hero__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-bottom: 40px;
    border-bottom: 1px solid color-mix(in srgb, var(--c-ink) 12%, transparent);
  }
  .exchange-hero__brand-main {
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: 22px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    margin: 0;
    color: var(--c-ink);
    text-align: left;
  }
  .exchange-hero__brand-sub {
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: 14px;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    margin: 4px 0 0;
    color: var(--c-muted);
    text-align: left;
  }
  .exchange-hero__brand-logo {
    height: 48px;
    width: auto;
    display: block;
    flex: 0 0 auto;
  }

  /* Corps centré verticalement dans l'espace sous le header. */
  .exchange-hero__content {
    flex: 1 1 auto;
    min-height: 0;
    display: flex;
    align-items: center;
    gap: 80px;
  }
  .exchange-hero__text {
    flex: 1 1 0;
    min-width: 0;
  }

  .exchange-hero__eyebrow {
    display: flex;
    align-items: center;
    gap: 12px;
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: 20px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    color: var(--c-muted);
    margin: 0 0 24px;
    opacity: 0;
    animation: ex-fade-left 0.7s cubic-bezier(0.16, 1, 0.3, 1) 0.12s both;
  }
  .exchange-hero__eyebrow-square {
    width: 14px;
    height: 14px;
    background: var(--c-accent);
    display: inline-block;
    flex: 0 0 auto;
  }

  .exchange-hero__title {
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: clamp(56px, 5.6vw, 92px);
    line-height: 1.05;
    font-weight: 600;
    letter-spacing: -0.02em;
    margin: 0;
    color: var(--c-ink);
    text-wrap: balance;
    text-align: left;
    opacity: 0;
    animation: ex-fade-up 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.28s both;
  }
  .exchange-hero__title-italic {
    font-family: 'Iowan Old Style', 'Palatino Linotype', Palatino, Georgia, 'Times New Roman', serif;
    font-style: italic;
    font-weight: 400;
  }

  .exchange-hero__subtitle {
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: 26px;
    line-height: 1.45;
    font-weight: 400;
    color: var(--c-muted);
    margin: 28px 0 0;
    max-width: 820px;
    opacity: 0;
    animation: ex-fade-up 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.42s both;
  }

  /* Liste optionnelle de points de discussion. */
  .exchange-hero__prompts {
    list-style: none;
    margin: 36px 0 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 18px;
    max-width: 820px;
    opacity: 0;
    animation: ex-fade-up 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.54s both;
  }
  .exchange-hero__prompt {
    display: flex;
    align-items: flex-start;
    gap: 18px;
    font-family: 'Hanken Grotesk', sans-serif;
    font-size: 24px;
    line-height: 1.4;
    font-weight: 400;
    color: var(--c-ink);
  }
  .exchange-hero__prompt-mark {
    flex: 0 0 auto;
    width: 12px;
    height: 12px;
    margin-top: 11px;
    background: var(--c-accent);
    display: inline-block;
  }

  /* Colonne droite : motif "discussion", icône dans un halo doré, centré. */
  .exchange-hero__visual {
    flex: 0 0 38%;
    position: relative;
    align-self: stretch;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .exchange-hero__halo {
    position: absolute;
    width: 460px;
    height: 460px;
    max-width: 90%;
    aspect-ratio: 1;
    border-radius: 50%;
    background: radial-gradient(circle, color-mix(in srgb, var(--c-accent) 30%, transparent) 0%, color-mix(in srgb, var(--c-accent) 0%, transparent) 68%);
    opacity: 0;
    animation: ex-scale-in 0.9s cubic-bezier(0.16, 1, 0.3, 1) 0.2s both;
  }
  .exchange-hero__icon {
    position: relative;
    display: block;
    width: clamp(240px, 22vw, 340px);
    height: clamp(240px, 22vw, 340px);
    color: var(--c-ink);
    opacity: 0;
    animation: ex-scale-in 0.9s cubic-bezier(0.34, 1.56, 0.64, 1) 0.34s both;
  }
  .exchange-hero__icon :global(svg) {
    width: 100%;
    height: 100%;
  }

  @keyframes ex-fade-up {
    from { opacity: 0; transform: translateY(26px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes ex-fade-left {
    from { opacity: 0; transform: translateX(-22px); }
    to { opacity: 1; transform: translateX(0); }
  }
  @keyframes ex-scale-in {
    from { opacity: 0; transform: scale(0.9); }
    to { opacity: 1; transform: scale(1); }
  }
</style>
