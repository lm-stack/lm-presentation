node_modules/
dist/
.astro/
.DS_Store
.env
.env.local

# OneDrive desktop.ini
desktop.ini

# Source archive (PDF, PPTX, RAW, unzips) - trop lourd pour git, conservé en local
source/

# Fichiers Cloudflare
.wrangler/
# Carte des thèmes générée au build (prebuild) depuis le frontmatter des decks
functions/scheme-map.ts
.superpowers/

# Artefacts de dev local (logs serveur, screenshots de diagnostic, tooling)
*.log
diag-shots/
.playwright-cli/
__shot.mjs

# Exports PDF locaux du skill /pdf (dossier pdf/ à la RACINE uniquement, ancré
# par le / initial : ne pas ignorer .claude/skills/pdf/). Jamais commités.
/pdf/
